{
  "name": "portfolio-mono",
  "version": "0.1.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "eslint": "^8.12.0",
    "prettier": "^2.6.1",
    "typescript": "^4.6.3"
  }
}